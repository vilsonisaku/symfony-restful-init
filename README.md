# symfony-restful-init

* ``` symfony console doctrine:database:drop --force ```
* ``` symfony console doctrine:database:create ```
* ``` symfony console doctrine:migrations:migrate ```
* ``` symfony server:start ```
